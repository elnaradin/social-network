

jwt:
  secret: skillbox
spring:
  security:
    filter:
      order: 10
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  liquibase:
    enabled: true
    change-log: classpath:/dbchangelog-master.xml

email:
  subject: Восстановление пароля
  message: "Чтобы восстановить пароль, перейдите по ссылке: http://localhost:80/change-password/"





